services:
  api:
    build:
      context: .
      target: development
    environment:
      NODE_ENV: development
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    restart: on-failure
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run start:dev"